//Appel de la fonction appMap implantée dans le script
function appMap() {
    //Stockage de l'objet App dans la variable app_
    let app_ = Object.create(App);
    //Éxécution de la fonction
    app_.App_run();
}